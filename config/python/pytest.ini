[pytest]
# Disable cache to prevent permission issues
addopts = -p no:cacheprovider

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths - only bulletin board tests
testpaths = tests/bulletin_board tests

# Coverage settings for bulletin board
coverage_paths = packages/bulletin_board
